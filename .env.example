# Copy this file to .env and customize the values before running the app (via npm or docker-compose).

# Port exposed by the application (server listens on 5000 internally)
PORT=5000

# Database selection
#   DB_TARGET=local   -> use LOCAL_DATABASE_URL (default for running `npm run dev`)
#   DB_TARGET=docker  -> use DOCKER_DATABASE_URL (default inside docker-compose where the DB host is `db`)
#   DB_TARGET=railway -> use RAILWAY_DATABASE_URL (copy from Railway dashboard to test against prod data)
DB_TARGET=local

# Local machine connection string (Postgres must be running on your host)
LOCAL_DATABASE_URL=postgres://postgres:postgres@localhost:5432/crossword_local

# In-container connection string (used by docker-compose where the DB host is `db`)
DOCKER_DATABASE_URL=postgres://postgres:postgres@db:5432/crossword_local

# Optional remote database (e.g., Railway). Leave blank locally and paste the secret when you need it.
RAILWAY_DATABASE_URL=

# Backwards-compatible fallback used by tools that only read DATABASE_URL. Leave blank locally.
DATABASE_URL=

# Postgres container credentials (used by docker-compose)
POSTGRES_DB=crossword_local
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_PORT=5432

# Session secret for express-session cookies.
SESSION_SECRET=change-me-in-production
# Set to true only when serving over HTTPS behind a proxy/SSL terminator.
SESSION_COOKIE_SECURE=false

# Default admin account seeded on first boot. Update these in production.
DEFAULT_ADMIN_EMAIL=mark@example.com
DEFAULT_ADMIN_PASSWORD=super-secure-password
DEFAULT_ADMIN_NAME=Mark
